package com.sprachbruecke.translator.ui.theme

import androidx.compose.ui.graphics.Color

// Aktives Feld – Dezenterer Fokus
val ActiveFieldBackground   = androidx.compose.ui.graphics.Color.Transparent
val ActiveFieldBorder       = androidx.compose.ui.graphics.Color(0xFFFFD600)  // Kräftiges Gold-Gelb

// Trennlinie-Indikatoren
val CloudModeColor          = Color(0xFF1976D2)  // Blau – Cloud aktiv
val OnDeviceModeColor       = Color(0xFF388E3C)  // Grün – On-Device aktiv
val ErrorModeColor          = Color(0xFFD32F2F)  // Rot – kein Netz/Fehler

// Sprechende Person hervorheben
val SpeakerTopColor         = Color(0xFF1565C0)  // Dunkelblau für Person A
val SpeakerBottomColor      = Color(0xFF2E7D32)  // Dunkelgrün für Person B

// Trennlinie
val DividerColor            = Color(0xFFBDBDBD)  // Material Grey 400
